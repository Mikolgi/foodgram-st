apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
{{- range $key, $val := .Values.apiSecrets }}
  {{ $key }}: {{ $val | b64enc | quote }}
{{- end }}
